function Notes7() {
  return (
    <section>
      <p>
        We saw that if we use memo then child component not render if its props
        not change, but what happen when we pass a event handler as a props.
      </p>
    </section>
  );
}

export default Notes7;
